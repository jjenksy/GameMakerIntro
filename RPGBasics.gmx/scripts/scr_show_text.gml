///scr_show_text

//itterate over the loop of text values
   
    current_alpha -= 0.05;
    
    draw_text_colour(x,y-sprite_get_height(sprite_index), text,colour,colour,colour,colour,current_alpha);
    

